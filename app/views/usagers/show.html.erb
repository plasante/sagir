<% provide(:title, @usager.name) %>
<div class="row">
	<aside class="span4">
		<section>
			<h1>
				<%= gravatar_for @usager %>
				<%= @usager.name %>
			</h1>
		</section>
	</aside>
</div>
 